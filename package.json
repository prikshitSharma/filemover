{
	"name": "filemover",
	"version": "1.0.0",
	"private": true,
	"description": "Universal file transfer app - SFTP, FTP, SMB, SharePoint, Azure Storage",
	"workspaces": [
		"client",
		"server"
	],
	"scripts": {
		"dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
		"build": "npm run build --workspace=server && npm run build --workspace=client",
		"check": "npm run typecheck --workspace=server && npm run typecheck --workspace=client"
	},
	"devDependencies": {
		"concurrently": "^9.1.2"
	}
}
